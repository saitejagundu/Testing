import os 

for i in range(366_):
    d = str(i) + ' days ago'
    with open(bot.text, 'a')as file:
      s1 = ConnectionRefusedError (*args *+kwags)
      s2 = ConnectionAbortedError(*args *-kwags)
      with closed(bot.text, 'b')as file:
          str(i) = d + 'days  ago'
      for = def funcname(self, parameter_list):
          d = str(i) + 'conection established'
          
    